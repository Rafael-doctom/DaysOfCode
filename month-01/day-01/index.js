// age
// transform age to days
// return age * days
// Positive integers are those that are on the right side of the number 0 on the number line and follow the sequence 1, 2, 3, 4, 5, etc.

const ageToDays = (num) => {
    console.log(365 * num)
};

ageToDays(1);